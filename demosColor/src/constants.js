/**
 * Central module for application-wide constants
 */

// Visual thickness for outlines, rings, and tubes across color spaces
export const outlineEdgeThickness = 0.01; // Default thickness for outline elements

// Arrow dimensions for all color spaces
export const arrowRadius = 0.03; // Radius of arrow cone heads
export const arrowLength = 0.06; // Height/length of arrow cone heads

// Axis properties
export const axisThickness = 0.005; // Thickness of coordinate axes

// HSL specific constants
export const L_MID_POINT = 0.5;
export const MAX_VISUAL_RADIUS_AT_MID = 0.5;

// Other constants can be added here as needed

export const initialModel = 'HSL';
